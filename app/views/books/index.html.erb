<h2>All the books</h2>
<% if current_user.admin %>
    <%= link_to "Add a title", new_book_path %>
<% end %>

<% @books.each do |book| %>
    <li><%= link_to book.title, book_path(book) %></li>
<% end %>
